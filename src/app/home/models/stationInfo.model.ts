export interface StationInfo {
  stationId: string;
  stationName: string;
  arrivalDate: string;
  departureDate: string;
  stopDuration: number;
  firstStation: boolean;
  lastStation: boolean;
  userStation: boolean;
}
