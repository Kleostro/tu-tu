import { SetBtnIconsPipe } from './set-btn-icons.pipe';

describe('SetBtnIconsPipe', () => {
  it('create an instance', () => {
    const pipe = new SetBtnIconsPipe();
    expect(pipe).toBeTruthy();
  });
});
